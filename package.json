{
  "name": "@bojalelabs/core",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "svelte-normalize-css": "^1.0.0"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*",
      "playgrounds/*"
    ]
  },

  "scripts": {
    "build": "yarn lerna run build",
    "dev": "yarn lerna run dev"
  }
}
